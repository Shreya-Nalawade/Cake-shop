<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Cake Shop</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1>Welcome to Cake Shop</h1>
  <a href="/upload">Add New Cake</a>

  <div class="cake-list">
    <% cakes.forEach(cake => { %>
      <div class="cake-card">
        <img src="<%= cake.image %>" alt="<%= cake.name %>" width="200">
        <h2><%= cake.name %></h2>
        <p>💰 Price: ₹<%= cake.price %></p>
        <p>📦 Quantity: <%= cake.quantity %></p>
        <p>Status: <%= cake.inStock ? "In Stock" : "Out of Stock" %></p>

        <form action="/delete/<%= cake.id %>" method="POST">
          <button type="submit">🗑 Delete</button>
        </form>
      </div>
    <% }) %>
  </div>
</body>
</html>
